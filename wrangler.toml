name = "thecozytrio"
compatibility_date = "2026-04-06"

# Angular 21's @angular/build application builder writes the browser
# bundle (index.html + js + css) to dist/thecozytrio/browser/.
pages_build_output_dir = "dist/thecozytrio/browser"
